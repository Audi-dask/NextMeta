# 服务端口
server:
  port: ":8080" 

# 数据库连接配置
database:
  host: "mysql"                  # Docker Compose 内通过服务名访问 MySQL
  port: "3306"                   # 数据库端口
  user: "root"                   # 数据库用户名
  password: "4oOQq1ul0GNCpHn9"   # 数据库密码（请修改为强密码，需与 docker-compose.yaml 的 MYSQL_ROOT_PASSWORD 一致）
  dbname: "nextmeta"             # 数据库名称

# JWT 有效期统一使用分钟。expires 为 access token 有效期，refresh 为 refresh token 有效期。
jwt:
  secret: "o7nhR3guXCDz23QN"
  expires: 60
  refresh: 720

